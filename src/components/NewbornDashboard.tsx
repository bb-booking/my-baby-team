import { useRef } from "react";
import { useFamily } from "@/context/FamilyContext";
import { getKnowledgeCards } from "@/lib/phaseData";

// NewbornDashboard is now minimal — QuickLog, Sleep, Tasks, Leaps moved to Dashboard
export function NewbornDashboard() {
  return null;
}
